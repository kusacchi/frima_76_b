= render "devise/shared/header", resource: resource

%body
  .password-content
    .password-content__title
      %h2 パスワードをお忘れの方
    .password-content__form
      = form_with model: @user, url: user_password_path, id: 'new_user', class: 'new_user', local: true do |f|
        = render "devise/shared/error_messages", resource: resource
        .field
          %label{:for => "email"} メールアドレス
          %br/
        = f.email_field :email, autofocus: true, autocomplete: "email", :placeholder => "   ご登録されたメールアドレス", class: "input-text"
        .form-text
          %p
            ご登録されたメールアドレスにパスワード再設定のご案内が送信されます。
        .actions
          = f.submit "送信する", class: "btn"

= render "devise/shared/footer", resource: resource

