= render "devise/shared/header", resource: resource
%body
  .signup-form
    .signup-form__header
      %h2 会員情報入力
    .signup-form__form
      = form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f|
        = render "devise/shared/error_messages", resource: resource
        .field
          .field__inner
            %label{:for => "nickname"} ニックネーム
            %span
              必須
            %br/
            / サーバーサイドが未実装のため一時的に :emailとしています
            = f.text_field :email, :placeholder => "例) フリマ太郎", class: 'input-text'
        .field
          .field__inner
            %label{:for => "email"} メールアドレス
            %span
              必須
            %br/
            = f.email_field :email, autofocus: true, autocomplete: "email", :placeholder => "PC・携帯どちらでも可", class: 'input-text'
        .field
          .field__inner
            %label{:for => "password"} パスワード
            %span
              必須
            %br/
            = f.password_field :password, autocomplete: "new-password", :placeholder => "6文字以上の半角英数字", class: 'input-text'
        .field
          .field__inner
            %label{:for => "password_confirmation"} パスワード再入力
            %span
              必須
            %br/
            = f.password_field :password_confirmation, autocomplete: "new-password", :placeholder => "6文字以上の半角英数字", class: 'input-text'
            %p
              .inner-message
                ※英語と数字の両方を含めて設定してください
        .form-group
          .form-group__title
            %h3
              本人確認
          .form-group__text
            %p
              安心・安全にご利用いただくために、お客様の本人情報の登録にご協力ください。他のお客様に公開されることはありません。
        .field
          .field__inner
            %label{:for => "family_name"} お名前(全角)
            %span
              必須
            %br/
            / サーバーサイドが未実装のため一時的に :emailとしています
            = f.text_field :email, :placeholder => "  例) 山田", class: 'input-text-harf'
            = f.text_field :email, :placeholder => "  例) 彩", class: 'input-text-harf'
        .field
          .field__inner
            %label{:for => "family_name_kana"} お名前カナ(全角)
            %span
              必須
            %br/
            / サーバーサイドが未実装のため一時的に :emailとしています
            = f.text_field :email, :placeholder => "  例) ヤマダ", class: 'input-text-harf'
            = f.text_field :email, :placeholder => "  例) アヤ", class: 'input-text-harf'
        .field
          .field__inner
            %label{:for => "birthday"} 生年月日
            %span
              必須
            %br/
            .input-date
              != sprintf(f.date_select(:birthday, prefix:'birthday',with_css_classes:'XXXXX', prompt:"--",use_month_numbers:true, start_year:Time.now.year, end_year:1900, date_separator:'%s', class: "date"),'年','月')+'日'
            %p.text
              ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
        .actions
          = f.submit "登録する", class: 'btn'

= render "devise/shared/footer", resource: resource